#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#define M(a,x) memset(a,x,sizeof(a));
#define INF 100000
int g[8][8];
int ans[8];
int main(){
	M(g,0);
	M(ans,INF)
	int n,m;
	scanf("%d %d",&n,&m);
	
}
